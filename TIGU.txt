#include <iostream>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    int years = 0;
    while (n < m) {
        int interest = (n * 10 + 50) / 100; // 10% lãi, làm tròn đúng
        n += interest;
        years++;
    }
    cout << years << endl;
    return 0;
}
