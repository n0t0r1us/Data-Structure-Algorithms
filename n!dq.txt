#include <iostream>
using namespace std;

// Hàm d? quy tính giai th?a
int giaithua(int n) {
    if (n == 0 || n == 1) return 1;
    return n * giaithua(n - 1);
}

int main() {
    int n;
    cin >> n;

    int kq = giaithua(n);
    cout << kq << endl;

    return 0;
}
