#include <bits/stdc++.h>  
using namespace std;
long long a[100005],i,j,k,sum,n;
int main()
{
 cin>>n>>k;
 for (i=1;i<=n;i++) 
 cin>>a[i];
 sort(a+1,a+1+n,greater<int>());
 j=1,sum=a[1];
 for (i=2;i<=n;i++){
  if (a[i]+k<=a[j])
  j++;
  else 
  sum+=a[i];
 }
 cout<<sum;
}