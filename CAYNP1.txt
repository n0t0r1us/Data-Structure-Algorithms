#include<iostream>
#include <vector>
using namespace std;
int n;
vector<int>a;
void Duyet(int i){
	if (i >= n) return;
	cout << a[i] << " ";
	Duyet(2 * i + 1);
	Duyet(2 * i + 2);
}
int main (){
	cin >> n;
	a.resize(n);
	for(int i = 0; i < n; i++)
	cin >> a[i];
	Duyet(0);
	cout << endl;
	return 0;
}